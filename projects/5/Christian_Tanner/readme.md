# Interactive Hello World with WebAssembly (C + Emscripten)

## 🧾 Description

This project takes a user's name, age, and repeat count, sends them to a C function compiled to WebAssembly, and returns a repeated greeting message in the browser.

---

## ⚙️ Setup Instructions

### 1. Prerequisites
- [Install Emscripten](https://emscripten.org/docs/getting_started/downloads.html)
- Activate the Emscripten environment:
```bash
source /path/to/emsdk/emsdk_env.sh
